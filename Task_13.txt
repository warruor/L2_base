function isCircleSorted( arr ){
  // pupupu
  if(arr.length==0) return true;
  let count=0;
  for(let i=0;i<arr.length-1;i++){
    if(arr[i]>arr[i+1]){
      count++;
    }
  }
  if(count==0) return true;
  if(count==1)return arr[arr.length-1]<=arr[0];
  return false;
}